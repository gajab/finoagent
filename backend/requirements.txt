fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
authlib>=1.3.0
itsdangerous>=2.1.2
cryptography>=42.0.0
httpx>=0.26.0
pydantic-settings>=2.1.0
yfinance>=0.2.36
# Pick & Shovel v2 — parse SEC/IR HTML and downloaded investor presentation PDFs
beautifulsoup4>=4.12.0
pypdf>=4.0.0
# Pick & Shovel v2 social signal — impersonates browser TLS to read StockTwits (Cloudflare-fronted)
curl_cffi>=0.7.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
python-multipart>=0.0.6
croniter>=2.0.0
aiosmtplib>=2.0.0
markdown>=3.5.0
asyncpg>=0.29.0
# Schema migrations — see MIGRATIONS.md
alembic>=1.13.0
# Sync psycopg2 driver used by Alembic against Cloud SQL Postgres
psycopg2-binary>=2.9.0
statsmodels>=0.14.0
scikit-learn>=1.3.0
fastdtw>=0.3.4
# Chart-pattern PNG export (annotated candlesticks). Detection itself is scipy/numpy.
matplotlib>=3.8.0
mplfinance>=0.12.10b0
# Institutional vol modeling — Heston calibration & Vanna-Volga greeks (SVI/RND are scipy)
QuantLib>=1.34
# Portfolio optimization — min-vol / HRP / DiscreteAllocation (pulls in cvxpy + solvers)
PyPortfolioOpt>=1.5.5
ibind[oauth]>=0.1.0
pycryptodome>=3.20.0
# GCP Secret Manager — optional, only used when GCP_PROJECT_ID is set
google-cloud-secret-manager>=2.20.0
# MCP — serves backend analytics as Model-Context-Protocol tools (app/mcp_server/*),
# mounted over Streamable HTTP at /mcp/<name>.
mcp>=2.0.0
